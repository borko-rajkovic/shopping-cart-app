export interface Product {
  // ID: string (UUID format)
  // Product name: string
  // Price: Floating Number (e.g. 10.99)
  // Quantity: number

  id: string;

  productName: string;

  price: number;

  quantity: number;
}
